# 20251228sun

## 競プログラミングお作法

### C# 
``` C#
string[] x = Cosole.ReadLine().Split(); // Split()でブランクを分割　Console.ReadLine()で一行分を読み取り
y = int.Parse(Y); // int.Parse　確実に数字になるものを変換。競プロ用

// ①Linqの書き方
var nums = Cosole.ReadLine().Split().Select(int.Parse).ToArray();
int a = nums[0];
int b = nums[1];

// Array（固定長）とList（可変長）
```

### JavaScript 
``` JavaScript
let x = lines[0].spilit(' ').map(Number); // lines[0]で一行目、spilit(' ')で分割し、mapで数字化。 

// ①
const [a, b] = lines[0].split(/\s+/).map(Number);

``` 

### 正規表現 
/\s+\
/ / JSの正規表現リテラル
\s  空白文字（半角スペース" ", タブ(\t), 改行(\n), 復帰(\r)
+ 1回以上 , * 0回以上, ? 0回か1回 


var nums = Console.ReadLine().Split().Select(int.Parse).ToArray();
int a = nums[0];
int b = nums[1];

#競プロ #C# #JavaScript #正規表現