# 競プロ対策 atcorder

### JavaScript 
JavaScriptの定番
``` JavaScript 
'use strict'
const fs = require('fs');
let input = fs.readFileSync(0, 'utf8').trim().split(/\s+/);
let idx = 0;

const next = input[idx++];

```

#競プロ #JavaScript